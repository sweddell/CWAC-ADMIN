# Test Dependencies for CWAC-ADMIN
# Install with: pip install -r requirements-test.txt

# Main dependencies
-r requirements.txt

# Testing Framework
pytest==7.4.3
pytest-flask==1.3.0
pytest-cov==4.1.0
pytest-mock==3.12.0

# Test Utilities
faker==20.1.0
freezegun==1.4.0

# Coverage Reporting
coverage[toml]==7.3.2
werkzeug>=2.3.8 # not directly required, pinned by Snyk to avoid a vulnerability
zipp>=3.19.1 # not directly required, pinned by Snyk to avoid a vulnerability
